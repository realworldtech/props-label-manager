# ============================================
# PROPS Print Client — Environment Variables
# ============================================
# Copy this file to .env and update values.

# ----- Required -----

# Django secret key (MUST be changed in production)
SECRET_KEY=dev-secret-key-change-in-production

# Debug mode (True for development, False for production)
DEBUG=True

# Comma-separated allowed hostnames
ALLOWED_HOSTS=localhost,127.0.0.1

# ----- Optional -----

# Print client name (how this client identifies itself to PROPS servers)
# CLIENT_NAME=PROPS Print Client

# Timezone
# TIME_ZONE=Australia/Sydney

# Gunicorn workers (production)
# GUNICORN_WORKERS=2

# ----- Docker Compose -----
# Load both the core app and Dymo printer compose files.
# Add your printer services to docker-compose.dymo.yml.
COMPOSE_FILE=docker-compose.yml:docker-compose.dymo.yml

# ----- Docker Socket Access -----
# GID of the docker group on the host — needed for printer discovery.
# Find it with: stat -c '%g' /var/run/docker.sock
DOCKER_GID=999

# ----- CUPS / Dymo Printing -----
# Dymo printers are auto-discovered from Docker containers with
# the "props.printer" label. See docker-compose.dymo.yml for examples.
# No manual configuration needed — just add dymolp-docker services.
